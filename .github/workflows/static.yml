name: Deploy static site to GitHub Pages

on:
  push:
分支: [ main ]  # 确保是你的主分支

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup and deploy
        run: |
          echo "Deployment successful!"
        # 这里可以根据你的需要添加具体的部署命令，比如构建、运行等
